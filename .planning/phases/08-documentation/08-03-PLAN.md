---
phase: 08-documentation
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified: [README.md]
autonomous: false

must_haves:
  truths:
    - "All internal links in README.md resolve to existing files"
    - "All internal links in CONTRIBUTING.md resolve to existing files"
    - "GitHub repo description and topics are updated via gh CLI"
    - "No broken references to non-existent files across all documentation"
  artifacts:
    - path: "README.md"
      provides: "Verified README with all links valid"
    - path: "CONTRIBUTING.md"
      provides: "Verified CONTRIBUTING with all links valid"
  key_links:
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "markdown link"
      pattern: "\\]\\(CONTRIBUTING\\.md\\)"
    - from: "README.md"
      to: "CODE_OF_CONDUCT.md"
      via: "markdown link or reference"
      pattern: "CODE_OF_CONDUCT"
    - from: "README.md"
      to: "LICENSE"
      via: "badge + text link"
      pattern: "\\]\\(LICENSE\\)"
    - from: "README.md"
      to: "SECURITY.md"
      via: "markdown link"
      pattern: "\\]\\(SECURITY\\.md\\)"
---

<objective>
Update GitHub repository metadata (description and topics) via gh CLI, verify all documentation cross-links are valid, and fix any broken references. This is the final quality gate for Phase 8.

Purpose: Ensure all documentation is internally consistent, all links work, and the repo presents well on GitHub (description, topics for discoverability).
Output: Updated repo metadata + verified documentation with zero broken links
</objective>

<execution_context>
@/Users/bragatte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bragatte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentation/08-CONTEXT.md
@.planning/phases/08-documentation/08-RESEARCH.md
@.planning/phases/08-documentation/08-01-SUMMARY.md
@.planning/phases/08-documentation/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix all documentation cross-links</name>
  <files>README.md, CONTRIBUTING.md</files>
  <action>
    Scan ALL documentation files for internal links and verify each target exists.

    **Files to scan:**
    - README.md
    - CONTRIBUTING.md
    - CODE_OF_CONDUCT.md
    - SECURITY.md
    - .github/ISSUE_TEMPLATE/bug_report.md
    - .github/ISSUE_TEMPLATE/feature_request.md
    - .github/pull_request_template.md

    **Verification steps:**
    1. Extract all `](path)` patterns from each file
    2. For each path, verify the target file exists (ignore URLs starting with http)
    3. Verify Table of Contents anchor links in README match actual heading text
    4. Check for any references to non-existent files: GUIDING-PRINCIPLES.md, REQUIREMENTS.md, docs/how-to-contribute-with-pull-requests.md
    5. Fix any broken links found

    **Also verify:**
    - No emoji characters in README.md, CONTRIBUTING.md, or templates
    - shields.io badge URLs are well-formed (no broken markdown)
    - Mermaid code block in README is syntactically valid
    - All `<details>` tags are properly closed

    Fix any issues found directly in the files.
  </action>
  <verify>
    - `rg '\]\(' README.md CONTRIBUTING.md` -- manually check each link resolves
    - `rg 'GUIDING-PRINCIPLES\|REQUIREMENTS\.md\|how-to-contribute' README.md CONTRIBUTING.md` returns nothing
    - No unclosed `<details>` tags
    - Badge markdown is valid
  </verify>
  <done>All documentation links are verified and working, no broken references, no emoji characters</done>
</task>

<task type="auto">
  <name>Task 2: Update GitHub repository metadata</name>
  <files></files>
  <action>
    Use `gh` CLI to update the GitHub repository metadata:

    **1. Update repository description:**
    ```bash
    gh repo edit BragatteMAS/os-postinstall-scripts \
      --description "Cross-platform post-install automation for Linux, macOS & Windows. One command to set up your entire dev environment with 10+ installer types, 3 profiles, dry-run mode, and production-grade error handling."
    ```

    **2. Update repository topics (20 tags):**
    ```bash
    gh repo edit BragatteMAS/os-postinstall-scripts \
      --add-topic bash,powershell,shell-script,automation,devops,dotfiles,post-install,setup-script,cross-platform,linux,macos,windows,apt,homebrew,winget,flatpak,snap,developer-tools,ai-tools,mcp
    ```

    **3. Document socialify steps** (manual operation, not automated):
    Add a note in the plan summary that the social preview image should be generated at:
    `https://socialify.git.ci/BragatteMAS/os-postinstall-scripts`
    and uploaded via GitHub Settings > Social preview. This is a one-time manual operation.

    If `gh` CLI is not authenticated or fails, record the commands for the user to run manually.
  </action>
  <verify>
    - `gh repo view BragatteMAS/os-postinstall-scripts --json description` shows updated description
    - `gh repo view BragatteMAS/os-postinstall-scripts --json repositoryTopics` shows updated topics
  </verify>
  <done>GitHub repo description and topics are updated (or commands documented for manual execution if auth fails)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 8 documentation suite:
    - README.md (23-section complete rewrite)
    - CONTRIBUTING.md (project-specific, no BMAD boilerplate)
    - CODE_OF_CONDUCT.md (Contributor Covenant v2.1)
    - Updated GitHub templates (bug report, feature request, PR)
    - GitHub repo metadata (description + topics)
    - All cross-links verified
  </what-built>
  <how-to-verify>
    1. Open README.md on GitHub and verify:
       - Badges render correctly (6 badges in 2 rows)
       - Table of Contents links work (click each)
       - Mermaid diagram renders
       - Collapsible sections expand/collapse
       - No emojis in text
       - Demo GIF placeholder shows alt text
    2. Open CONTRIBUTING.md and verify:
       - No BMAD/Discord references
       - Style guide matches actual project patterns
       - ShellCheck requirement is prominent
    3. Open CODE_OF_CONDUCT.md and verify Contributor Covenant text
    4. Check repo description and topics on GitHub repo page
    5. Optionally: generate social preview at socialify.git.ci and upload
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. All `](...)` links in README.md point to existing files
2. All `](...)` links in CONTRIBUTING.md point to existing files
3. No references to GUIDING-PRINCIPLES.md, REQUIREMENTS.md, or other non-existent files
4. GitHub repo description updated (or commands documented)
5. GitHub repo topics updated (or commands documented)
6. No emoji characters in any documentation file
7. User has visually verified the rendered documentation on GitHub
</verification>

<success_criteria>
- Zero broken links across all documentation files
- GitHub repo metadata reflects the project accurately
- User has visually confirmed README renders correctly on GitHub
- All documentation files are consistent with each other
- Phase 8 is complete and ready for release
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentation/08-03-SUMMARY.md`
</output>
