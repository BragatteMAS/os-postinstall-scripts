# AI Tools Package List
# Source: src/install/ai-tools.sh
# One package per line, comments start with #
# Format: prefix:package-name (npm:, curl:, npx:, uv:) or bare word

# AI CLI Tools (via npm)
npm:@anthropic-ai/claude-code
npm:@openai/codex
npm:@google/gemini-cli

# Local LLM Runtime (via curl)
curl:ollama

# MCP Servers (via npx)
npx:@context7/mcp-server
npx:@modelcontextprotocol/server-fetch
npx:@modelcontextprotocol/server-sequential-thinking
npx:@modelcontextprotocol/server-fastapi
npx:@modelcontextprotocol/server-a2a
npx:@modelcontextprotocol/server-system-prompts-and-models-of-ai

# Python Tools (via uv)
uv:serena-mcp-server

# Development Utilities
# uv — installed by src/install/uv.sh (dev-env), listed here for reference only
