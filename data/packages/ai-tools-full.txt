# ai-tools-full.txt
# AI tools — full only (Bragatte uses heavily)
#
# DON'T run `./setup.sh full` if you want neutral dev defaults.
# Use `./setup.sh developer` instead.
#
# Format: prefix:package-name (npm:, curl:, bun:, uv:, pipx:)
#
# MCPs (Model Context Protocol) are managed externally via mcpl
# (MCP Launchpad = `mcpl`, installed below from its git repo; the PyPI `mcpl` is a different package).
# Run `h ai` for runtime help. See docs/migration-guide.md for machine moves.

# AI CLI client (via npm) — Claude Code comes from the brew cask, not npm
npm:@openai/codex@latest

# AI CLI alternative (via bun — opencode is bun-distributed)
bun:opencode-ai

# Utility (via uv — uv first, pipx only as fallback if uv ever fails)
# Note: fastapi-mcp removed — it's a Python library (no CLI executable), so
# `uv tool install` rejects it ("No executables are provided"). Library-only
# packages belong in per-project venvs, not as global tools.
uv:markitdown
# MCP Launchpad (mcpl) — from git; the installer derives the tool name from the URL for the idempotent check
uv:git+https://github.com/kenneth-liao/mcp-launchpad

# Personal npm globals (full only)
npm:@bitwarden/cli
npm:ccusage
npm:pptxgenjs
npm:docx
npm:firecrawl-cli
npm:@observablehq/framework
npm:gsd-pi
npm:prime-agent
npm:cline
npm:clawdbot
