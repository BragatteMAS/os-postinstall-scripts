# brew.txt
# Homebrew formulae — base (minimal + developer + full)
# Universal essentials. Other formulae live in brew-developer.txt or brew-full.txt.
#
# Naming convention:
#   brew.txt              base — included by every macOS profile (this file)
#   brew-developer.txt    developer + full
#   brew-full.txt         full only (Bragatte's personal pick)
#
# Cross-platform conceptual parity:
#   git, curl, wget, bash, zsh → apt has equivalents, winget has equivalents.
#   Linux/Windows lists currently legacy mono — see CHANGELOG "Known asymmetry".

# Network / file transfer
git
curl
wget

# Shells (bash 4+ required by setup.sh; zsh is the modern default)
bash
zsh
