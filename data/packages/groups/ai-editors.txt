# AI editors & desktop apps — Cursor, Claude (desktop + CLI rolling), Antigravity, T3 Code
# Note: only `claude-code@latest` (rolling channel) — `claude-code` (plain) is dropped to avoid the duplicate cask.
cursor
claude
claude-code@latest
antigravity
t3-code
