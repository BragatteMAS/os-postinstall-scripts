# apt.txt
# APT packages — base (minimal + developer + full)
# Universal essentials. Other packages live in apt-developer.txt or apt-full.txt.
#
# Naming convention:
#   apt.txt              base — included by every Linux profile (this file)
#   apt-developer.txt    developer + full
#   apt-full.txt         full only (Bragatte's personal pick)

# Version control
git
git-lfs

# Build essentials (universal compile chain)
build-essential
cmake
pkg-config
libssl-dev
make

# SSH access
openssh-server

# Modern shell
zsh
